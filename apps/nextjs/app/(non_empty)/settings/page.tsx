'use client';
import { useSearchParams, redirect } from 'next/navigation';
import { useEffect } from 'react';
import { useQuery } from '@apollo/client';
import { USERS, usersProps } from '@/graphql/queries/users';

export default function Page() {

  return <>
	</>
}
